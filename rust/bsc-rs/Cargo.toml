[package]
name = "bsc-rs"
version = "0.0.1"
edition = "2024"
license = "Apache-2.0"
description = "Memory-safe Rust port of libbsc (BWT + ST + LZP + QLFC). Work in progress."
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]

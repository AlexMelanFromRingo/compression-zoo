# Benchmarks

Placeholder. Will be filled in once each plugin DLL builds and runs.

Planned corpora: enwik8, enwik9, Calgary corpus, Silesia corpus.

Comparison columns:

- LZMA2 ultra (built-in 7-Zip baseline)
- ZPAQ levels 1, 3, 5
- libbsc default
- CMIX (one corpus only — too slow elsewhere)

Reported metrics: compressed size, encode time, decode time, peak RSS.
